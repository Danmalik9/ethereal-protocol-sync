import { describe, it, expect } from "vitest";
import { Tx, Account } from "@stacks/transactions";

describe("synchronizer-ledger", () => {
  // Tests to be implemented
  it("should be deployable", () => {
    expect(true).toBe(true);
  });
});
